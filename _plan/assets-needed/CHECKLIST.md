# Assets necesarios para el build

Marcá lo que tengas; lo que falte, HIVE lo genera como placeholder coherente.

## 1. Logo AIporvos
- [ ] SVG vectorial (ideal) o PNG fondo transparente, versión clara y oscura.
- Fallback HIVE: logotipo tipográfico en Syne (como hace VDG con su PNG).

## 2. Hero — animación Rive (`.riv`)
- [ ] Archivo `.riv` del personaje/robot AIporvos con state machine:
      inputs `mouseX`, `mouseY` (mirada sigue cursor) + estados `idle`/`blink`.
- Si no hay: HIVE crea un robot SVG animado propio o adapta un Rive libre de la comunidad,
      con el mismo *tipo* de efecto que daveholloway.uk.
- Fallback de carga: SVG estático del robot (no rompe el layout).

## 3. Casos — imágenes (carrusel)
- [ ] 3–6 imágenes de casos/clientes (16:9 o 1:1).
- Fallback: mockups generados (ilustración plana en paleta VDG).

## 4. Favicon
- [ ] `favicon.svg`. Fallback: derivado del logo.

## 5. Datos de contacto / redes (confirmar)
- WhatsApp: +54 260 484 4952 (confirmado).
- [ ] Email de contacto.
- [ ] Redes (Instagram / LinkedIn) si las hay.
