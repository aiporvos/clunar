---
---
<section id="inicio" class="hero-section relative overflow-hidden pt-28 pb-10 lg:pt-20 lg:pb-0 lg:min-h-screen lg:flex lg:flex-col lg:justify-center">
  <div class="w-full px-6 sm:px-10 lg:px-16">

    <div class="grid lg:grid-cols-[1.1fr_1fr] gap-8 lg:gap-16 items-center">

      <!-- LEFT: Headline + lead + CTAs -->
      <div class="flex flex-col items-start text-left order-1">
        <h1 class="font-display glow-text mb-6" style="font-size:clamp(2.5rem,7.5vw,4.8rem);line-height:0.98;letter-spacing:-0.02em">
          <span class="block whitespace-nowrap" data-split="load">Sistemas de IA</span>
          <span class="block whitespace-nowrap" data-split="load" data-accent-last>que trabajan.</span>
        </h1>

        <p class="max-w-md text-lg sm:text-xl animate-fade-up stagger-2" style="color:hsl(var(--muted-foreground))">
          Automatización e IA para empresas, <span class="font-semibold" style="color:hsl(var(--accent))">sin perder el control de tus datos.</span>
        </p>

        <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3 sm:gap-4 mt-8 animate-fade-up stagger-4">
          <a href="https://wa.me/542604844952"
             target="_blank"
             rel="noopener noreferrer"
             data-magnetic
             class="btn-brutal btn-shine group flex items-center gap-2 text-xs sm:text-sm px-6 sm:px-8 py-3"
             style="background:hsl(var(--primary));color:hsl(var(--primary-foreground))">
            Hablar con Claudio
            <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
          <a href="#soluciones"
             data-magnetic
             class="btn-brutal btn-shine text-xs sm:text-sm px-6 sm:px-8 py-3 flex items-center"
             style="background:hsl(var(--card));color:hsl(var(--foreground))">
            Ver servicios
          </a>
        </div>
      </div>

      <!-- RIGHT: Avatar duotono en blob orgánico -->
      <div class="flex items-center justify-center lg:justify-end order-2 animate-fade-up stagger-3">
        <div class="hero-blob-wrapper">
          <div class="hero-blob">
            <img class="hero-avatar-img" src="/clunar-cut.png" width="1024" height="1024"
                 loading="eager" alt="Claudio Luna" />
            <img class="hero-avatar-blink" src="/clunar-closed.png" width="1024" height="1024"
                 loading="eager" alt="" aria-hidden="true" />
          </div>
        </div>
      </div>

      <!-- STATS: fila completa debajo de ambas columnas -->
      <div class="order-3 lg:col-span-2">
        <div class="grid grid-cols-3 gap-3 sm:gap-4 max-w-lg animate-fade-up stagger-5">
          <div class="card-brutal text-left p-3 sm:p-4" style="background:hsl(var(--primary))">
            <p class="font-display text-2xl sm:text-4xl font-bold" style="color:hsl(var(--foreground))"><span data-count="15" data-prefix="+">+15</span></p>
            <p class="text-xs sm:text-sm mt-1 font-semibold" style="color:hsl(var(--foreground))">Años en sistemas</p>
          </div>
          <div class="card-brutal text-left p-3 sm:p-4" style="background:hsl(var(--success))">
            <p class="font-display text-2xl sm:text-4xl font-bold" style="color:#fff"><span data-count="100" data-suffix="%">100%</span></p>
            <p class="text-xs sm:text-sm mt-1 font-semibold" style="color:#fff">Datos bajo tu control</p>
          </div>
          <div class="card-brutal text-left p-3 sm:p-4" style="background:hsl(var(--accent))">
            <p class="font-display text-2xl sm:text-4xl font-bold" style="color:#fff">24/7</p>
            <p class="text-xs sm:text-sm mt-1 font-semibold" style="color:#fff">Operación medida</p>
          </div>
        </div>
      </div>

    </div>

    <div class="hidden lg:flex flex-col items-center mt-12 animate-fade-up stagger-5">
      <a href="#que-es" class="flex flex-col items-center gap-2 text-sm transition-colors hover:opacity-70" style="color:hsl(var(--muted-foreground))">
        <span>Conocer más</span>
        <svg class="w-4 h-4 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
        </svg>
      </a>
    </div>

  </div>
</section>

<style>
  /* El último carácter de la línea (el punto) va en accent */
  [data-accent-last] :global(.split-char-wrap:last-child .split-char) {
    color: hsl(var(--accent));
  }

  /* ── Avatar duotono en blob orgánico (adaptación zulik en clave de marca) ── */
  .hero-blob-wrapper {
    position: relative;
    width: min(78vw, 340px);
    transform: translate(var(--avatar-px, 0px), var(--avatar-py, 0px));
    transition: transform 0.35s cubic-bezier(.22,.68,0,1);
  }
  @media (min-width: 640px)  { .hero-blob-wrapper { width: min(60vw, 420px); } }
  @media (min-width: 1024px) { .hero-blob-wrapper { width: min(38vw, 500px); } }
  @media (min-width: 1536px) { .hero-blob-wrapper { width: 560px; } }

  /* La forma: blob orgánico con morph lento, borde y sombra de marca */
  .hero-blob {
    position: relative;
    aspect-ratio: 1;
    overflow: hidden;
    background: hsl(var(--primary));
    border: 3px solid hsl(var(--foreground));
    box-shadow: var(--shadow-brutal-lg);
    border-radius: 58% 42% 55% 45% / 48% 55% 45% 52%;
    animation: blob-morph 14s ease-in-out infinite, avatar-breathe 6s ease-in-out infinite;
    transform-origin: 50% 92%;
  }
  @keyframes blob-morph {
    0%, 100% { border-radius: 58% 42% 55% 45% / 48% 55% 45% 52%; }
    33%      { border-radius: 45% 55% 48% 52% / 56% 44% 56% 44%; }
    66%      { border-radius: 52% 48% 60% 40% / 44% 58% 42% 56%; }
  }
  @keyframes avatar-breathe {
    0%, 100% { scale: 1; }
    50%      { scale: 1.012; }
  }

  /* Duotono: la ilustración en escala de grises multiplicada sobre el amarillo */
  .hero-avatar-img, .hero-avatar-blink {
    position: absolute;
    inset: 4% 0 0 0;
    width: 100%;
    height: auto;
    display: block;
    filter: grayscale(1) contrast(1.08) brightness(1.04);
    mix-blend-mode: multiply;
  }
  .hero-avatar-blink {
    opacity: 0;
    animation: avatar-blink 6.5s ease-in-out infinite;
  }
  @keyframes avatar-blink {
    0%, 93%, 100% { opacity: 0; }
    95.5%, 97%    { opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-blob { animation: none; }
    .hero-avatar-blink { animation: none; }
    .hero-blob-wrapper { transition: none; }
  }
</style>

<script>
  const reducedMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;

  // ── Parallax sutil del avatar hacia el cursor (solo desktop) ──
  const avatarWrap = document.querySelector<HTMLElement>('.hero-blob-wrapper');
  if (avatarWrap && !reducedMotion && matchMedia('(pointer: fine)').matches) {
    let raf = 0;
    window.addEventListener('mousemove', (e) => {
      if (raf) return;
      raf = requestAnimationFrame(() => {
        const dx = e.clientX / window.innerWidth - 0.5;
        const dy = e.clientY / window.innerHeight - 0.5;
        avatarWrap.style.setProperty('--avatar-px', (dx * -14).toFixed(1) + 'px');
        avatarWrap.style.setProperty('--avatar-py', (dy * -8).toFixed(1) + 'px');
        raf = 0;
      });
    }, { passive: true });
  }

  // ── Contadores de stats ──
  document.querySelectorAll<HTMLElement>('[data-count]').forEach(el => {
    const target = parseInt(el.dataset.count || '0', 10);
    const prefix = el.dataset.prefix || '';
    const suffix = el.dataset.suffix || '';
    if (reducedMotion) { el.textContent = prefix + target + suffix; return; }
    const t0 = performance.now();
    const dur = 1300;
    function tick(t: number) {
      const p = Math.min((t - t0) / dur, 1);
      const eased = 1 - Math.pow(1 - p, 3);
      el.textContent = prefix + Math.round(target * eased) + suffix;
      if (p < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  });
</script>
